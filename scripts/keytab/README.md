# README

```powershell
.\Generate-Keytab.ps1 `
  -SamAccountName "realm-proxy" `
  -Realm "EXAMPLE.COM" `
  -Password "SuperSecret123!" `
  -OutputPath "C:\temp\realm-proxy.keytab"
```

```bash
./gen-keytab.sh 
